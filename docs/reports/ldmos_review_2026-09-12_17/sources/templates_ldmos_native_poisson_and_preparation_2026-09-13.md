# LDMOS 原生 Poisson 几何与电热准备复用

证据目录：`reference_staging/templates_ldmos_native_poisson_20260913/`。
本阶段承接B4 R3，依次验证原生Poisson几何、局部场差异与性能实现；不转移
B4完整曲线资格。N1和最终P3均已独立完成双栅压62点原门限验收。

## N1 原生 Poisson 几何

新增显式 `templates_ldmos_native_poisson.py`，与准备入口的
`--native-poisson-debug` / `--native-poisson-export` 参数配套使用。按已审计的
T-2022.03局部槽映射，将材料加权AverageBox边系数和硅Measure电荷体积一起
传入四方程接口。复合源体积、输运系数、热几何、材料常数和门限未变化。
逐节点坐标、单元连通性、材料及边集合均验证；网格10241节点、19782个Tri3，
其中硅5723节点/10515单元，另有两个SiO2区域。坐标um转SI，电流A/m，热流W/m。

冻结N1程序：UCRT64 Release `-O3 -DNDEBUG`，Eigen SparseLU/COLAMD，SHA256
`f09189ec30076a623df9ce9acbea19aedc309906dc364ed624df92bd206afada`。
967个源文件快照及哈希保存在 `binary_n1/`。新增方向导数诊断仅在显式请求时
做双侧残差评价，不参与Newton决策。原准备默认仍可复现B4。

准备及电热入口14项Python回归通过；含非均匀电荷体积/边系数的完整耦合
逐列Jacobian回归、硅电阻自热/有限接触回归通过。构建最初使用了不存在的
独立测试目标，查询CMake后改用实际的 `test_mobility`，构建成功。

## 原生固定状态残差与 Jacobian

在两个原生40 V热态上，仅组装，不做Newton；扣除有限接触电势重投影造成的
线性通量变化。自由硅节点5674个，Poisson残差单位C/m：

| N1 固定状态 | Vg4 L2 | Vg8 L2 |
|---|---:|---:|
| Vela本构密度 | 2.21916e-10 | 3.02574e-10 |
| 诊断中仅换为原生密度 | 2.54215e-14 | 2.63361e-14 |

该结果与上轮独立几何反事实重建吻合；本轮已将几何实际接入求解输入。
原生密度替换仅用于解释残差，不在自洽求解中注入原生载流子。

完整四方程方向导数采用有界方向，电势/QF方向量级1 V，温度方向量级100 K。
Vg8的电子连续性差分在h=1e-6时相对L2误差1.85e-4；缩至2.5e-7后降至
1.32e-5（约14倍下降），通过既有3e-5校验值，未放宽阈值。Vg4缩步后为
2.95e-6。其余块均低于9e-9。较大步长失败记录保留，呈现差分步长收敛，
不把它误记为首次全部通过。证据为 `n1_native_jacobian*_summary.json` 和
`n1_residual_identity.json`。

## 自洽代表点与完整曲线

每栅压Vd=0、1.333333、13.333333、40 V的独立N1代表点均通过原数值与热学门限。
非零点电流误差如下，不能替代完整31点指标：

| Vd / V | Vg4误差 / % | Vg8误差 / % |
|---|---:|---:|
| 1.333333 | 0.0038813 | 0.0052393 |
| 13.333333 | 0.0209170 | 0.0017675 |
| 40 | 0.0063384 | 0.0015837 |

新零压耦合重闭合首次因极小QF噪声未通过严格零功率门限。保留其已闭合的
电势后，仅在所有中性接触偏压为零时恢复平坦QF=0及T=300 K，再按完整原
四方程、原有限空穴边界验收；两个栅压均0次更新通过，KCL、热源、边界热流
均为零。没有把近零电流当成精确零功率，也没有放松逐行门限。
证据为 `n1_zero_projected/` 和 `n1_points_score.json`。

N1双栅压从各自合格零压种子完成完整扫描，策略沿用growth=12与显式全场
线性预测，证据为 `n1_full_vg4/`、`n1_full_vg8/`、`n1_full_final.json`。
62点通过原电学、数值及批准热学联合门限：

| N1完整曲线 | Vg4 | Vg8 |
|---|---:|---:|
| 最大电流误差 / % | 0.0240871 | 0.00714390 |
| 最大峰温误差 / K | 0.00940075 | 0.0419821 |
| 最大温度场RMS误差 / K | 0.00630804 | 0.0108474 |
| Newton更新 | 406 | 318 |
| 拒绝推进 | 1 | 0 |

## 新几何的局部场

40 V代表点的最大导带差由B4的0.38763/0.46211 eV降至0.00780/0.02665 eV。
界面电子密度绝对相对差中位数由2.30%/5.77%降至0.00913%/0.03526%，
空穴由7.76%/7.70%降至0.35589%/0.06181%。界面指Si/oxide共享节点；仅计
参考密度大于各场峰值1e-12的节点，筛选口径与B4一致。
不同版本的最大偏差位置可能变化。新Vg8最大导带差点的电势/QF对数贡献
仍明显相抵，最终电子密度偏差约0.07546%，不能单凭最大带边差推算电流误差。
来源为 `field_decomposition_points_summary.json` 及逐节点CSV；完整路径已重做
双栅压对照，`field_decomposition_full_summary.json`确认相同的改善。默认
Fermi/BGN模型未因本次几何修正发生改变。

## 连续性余差的源项隔离

在两个原生40 V固定状态上，保持N1输运通量不动，分别使用Vela/原生SRH+Auger，
组合原重心源体积、原生全域Measure及原生硅Measure，形成六组诊断。原生复合率
从cm^-3/s转为m^-3/s，保留有限接触电势重投影；另报告排除接触相邻节点的结果。
电子/空穴残差L2分别为Vg4的4.18252013/0.536018366 A/m、Vg8的
6.71813705/0.791463956 A/m，六种源项组合对L2的最大相对影响约4e-9。
精确Si/oxide共享界面对电子残差平方和的贡献为21.05%/18.83%，空穴为
50.83%/50.48%。主要固定状态余差在输运通量侧；该总体范数结论不排除微弱
电流行的源项影响，原逐行门限继续保留。证据：`continuity_source_isolation.json`。

## P1 温度物性准备复用

新增显式 `reuse_physics_preparation=true`，默认关闭。按节点准备只依赖掺杂的
固定300 K BGN；温度完全相同且掺杂不变时复用能带、态密度、寿命和Auger温度
多项式及其解析温度偏导。中性接触仍使用原70次二分求根，每次候选仅计算所需
载流子密度与导数，不复用旧温度的根。温度或掺杂变化会刷新对应准备；异源模型
或不匹配的温度/掺杂准备对象被拒绝。没有改动物理系数或接受门限。

P1冻结程序SHA256为`aafcad7be20956800b8899cc09fceefa93739c610749a9bb1e3c0ba19f2cb096`，
见`binary_p1/`。12项相关CTest通过；两栅压各零压、低压、高压热态和原生方向
导数共8组同输入重放，状态、残差、轨迹、接触量、门限和Jacobian审计逐项完全
相等，见`p1_replay_summary.json`。每子进程的Newton阶段计数范围内，反Fermi
函数调用由18678×组装次数降为11368；该计数不包含之前的初始边界投影和之后
的额外方向差分审计。掺杂准备累计5723次，后续温度准备按实际状态刷新。
早期非零代表点组装墙钟显示较大下降，但N1全曲线与编译/诊断存在负载重叠，
最终不采用这些百分比作为稳定加速结论。Vg4全曲线中，P2成功推进的CPU合计
仅比N1下降约3.97%，主要CPU节约来自提前截断失败长尾；后续以串行同输入
计时和新增IALMob热点对照区分各项收益。

## P2 停滞与SparseLU对照

N1的Vg4首次2.666667→4 V推进耗尽60次更新，CPU328.21875 s，进程wall
342.1003144 s；第8次以后更新alpha约1e-6，单步残差改善极小。候选停滞检测
只对同一迭代行缩放下的before/after比较：连续5次已接受更新同时满足
before>1e-6、alpha<1e-4、相对改善<1e-3时提前拒绝，交给已有外层减半步长。
靠近最终门限的逐行闭合不触发这项检测；该规则不是新的接受条件，默认关闭。

SparseLU候选只在压缩矩阵维度和outer/inner索引逐项相等时复用结构分析，
数值分解每次执行。结构变化、同非零数不同位置、未压缩输入及数值变化均有
回归；停滞窗口对有效进展、近收敛和关闭状态的行为也已回归通过。固定失败
尝试、代表点隔离及完整曲线复核均已完成，汇总见后文。

P2组合程序已冻结在`binary_p2/`，SHA256为
`2c75a1a40fe3812a2ef09fdebd07a4499b5245d29beceb3597636d2978971ff2`，
三个开关均为显式实验选项。五组隔离重放通过：停滞检测单独开启及叠加结构
复用均在第12次更新退出，前缀与原失败60次轨迹完全相同；叠加后符号分析
12→1次。Vg4/Vg8高温代表点分别保留12/7次更新，符号分析均为1次，
状态、残差、轨迹和原门限逐项一致。默认关闭结构复用的新版工具也单独回放
Vg8高温点，仍与N1完全一致。证据：`p2_replay_summary.json`。

历史B4 R3和N1各自双栅压74次尝试的离线窗口审计中，2/3/5/8次窗口均未命中最终成功
的尝试，仅命中Vg4已知失败尝试。5次窗口保留最初短暂缓慢、随后明显进展的
阶段，在持续停滞后退出；该离线审计不假设改变步长后会沿原轨迹继续，实际
组合曲线已验证并保留全部接受状态一致性。证据：`stall_window_audit.json`。

最终P2源代码完成全目标Release构建，完整CTest 807/807通过，127.26 s。
`p2_build_all.log`、`p2_ctest_all.log`保存构建与测试记录。运行目录、程序、
源快照及每版曲线输入清单均独立保留；这些实验开关不修改生产DC默认求解器。

## P3：由采样确认的IALMob温度屏蔽准备

为解释P2收益受限，复用独立的UCRT64 Release/gprof构建，在相同Vg8/40 V
高温重闭合输入上采样。7次更新后状态、残差和轨迹与非插桩程序完全一致。
`gprof_hot_vg8/gprof.txt`的平坦样本总计8.21 s，过程CPU49.046875 s，
`screeningMinimum`占可归因平坦样本31.06%。该比例不是整个过程CPU占比，
调用图中的内联/符号归属也不作为准确调用计数；采样仅用于选择进一步实测对象。

源代码确认：非300 K的屏蔽极小值由同一100次几何二分求得，只依赖相对有效
质量和温度；此前标量/自动微分、单元电学/温度列及相邻单元反复求解它。
新增显式 `reuse_ialmob_screening=true`，在一次输运状态准备内以精确
`(mass, temperature_K)`为键复用根，结束时销毁缓存。不同温度不分桶、不近似；
不同质量分别求解；300 K原预存路径保留。未改变G函数、根算法、迁移率公式
或包络温度导数。共享等温入口默认不启用该选项。

新增计数器独立核实实际求根次数。在同一P3程序的Vg8/40 V开关重放中，
Newton阶段求根2775960→125906次；7次更新及全部状态/残差/导数保持一致。
对应CPU54.234375→17.390625 s仍属于有后台任务的初步观测，最终串行计时另列。
源和程序冻结于`binary_p3/`，程序SHA256：
`376dd81f6040b9f6428dd137c476de31e429d16f167766ac42a7e62560fc4962`。

31项相关CTest通过，包括相邻浮点温度/质量键、局部全部偏导及缓存开启的
四方程温度列独立差分。9组重放（8个N1零压/低压/高温/原生Jacobian输入及
1个新版默认关闭筛选根缓存的控制）逐项相等。全目标Release构建及完整
CTest 808/808通过，142.90 s。正式程序`-O3 -DNDEBUG`，gprof关闭；插桩程序
独立保留。P3完整双栅压曲线已按原门限独立通过。

## 完整曲线的物理一致性与工作量

N1、P2、P3分别从合格零压种子执行Vg=4/8 V、Vd=0–40 V的31个精确点，
每版62点均通过原门限。P2/P3相对N1的全部精确点以及全部接受中间点
（Vg4含零压37点、Vg8含零压36点）的状态、残差、接触量和Newton轨迹
逐项完全相等；唯一截断的是原本失败的尝试，不转移任何失败状态的资格。
证据：`full_work_comparison.json`、`progress_identity_p2.json`、
`progress_identity_p3.json`和各版`*_full_final.json`。

| 版本 | Vg / V | Newton更新 | 拒绝尝试内更新 | 符号分析 | 子进程累计墙钟 / s | 子进程累计CPU / s |
|---|---:|---:|---:|---:|---:|---:|
| N1 新几何基线 | 4 | 406 | 60 | 406 | 3057.72 | 2788.67 |
| P2 准备、停滞、结构复用 | 4 | 358 | 12 | 38 | 2510.08 | 2472.75 |
| P3 再加屏蔽根复用 | 4 | 358 | 12 | 38 | 1250.68 | 870.95 |
| N1 新几何基线 | 8 | 318 | 0 | 318 | 2386.35 | 2331.48 |
| P2 准备、停滞、结构复用 | 8 | 318 | 0 | 36 | 2966.07 | 2487.47 |
| P3 再加屏蔽根复用 | 8 | 318 | 0 | 36 | 623.52 | 602.48 |

数值分解次数始终等于Newton更新次数；结构复用没有省略数值分解。
各版Vg4均36次非零接受推进、1次拒绝，Vg8均35次非零接受推进、无拒绝。
P3 Vg4/Vg8的Newton阶段反Fermi调用分别431984/409248次，精确温度屏蔽根
求解7485684/6867600次。计数范围与上文一致。

本表墙钟为求解子进程累计时间，不含父运行器读写、验收和结果整理，不能
称为完整端到端时间。全曲线运行阶段存在其他编译、诊断或曲线负载，特别是
P2 Vg8墙钟增加不能解释为算法退化；各项收益另用无其他托管重任务的串行
同输入重放测量。CPU合计也保留原始观测，不作严格硬件归一化比较。

## 同输入串行性能对照

所有完整曲线、编译和重分析结束后，对同一Vg=8 V/Vd=40 V重闭合输入串行
执行N1→P1→P2→P3→P3→P2→P1→N1。每版两次，均7次Newton更新，原门限
通过，全部状态、残差及轨迹与N1逐项相等。期间无其他本任务管理的重工作，
但不声称操作系统及其他程序完全空闲。墙钟在子进程结束时采集，父进程随后
进行的一致性检查不计入。证据：`benchmark_serial_summary.json`及
`final_performance_summary.json`。

| 版本 | 墙钟中位数 / s | 两次墙钟范围 / s | CPU中位数 / s | 两次CPU范围 / s |
|---|---:|---:|---:|---:|
| N1 新几何基线 | 53.83 | 50.88–56.79 | 44.38 | 43.83–44.94 |
| P1 准备复用 | 58.99 | 52.28–65.71 | 44.85 | 43.84–45.86 |
| P2 再加结构复用 | 57.02 | 56.98–57.05 | 44.70 | 44.19–45.22 |
| P3 再加屏蔽根复用 | 20.24 | 19.84–20.63 | 15.11 | 14.88–15.34 |

P3相对N1中位墙钟减少62.41%，CPU减少65.96%；相对P2分别减少64.51%和
66.20%。组装墙钟中位数由N1的46.12 s降至11.73 s。只有两次测量，不将
小幅差异解释为统计显著收益：P1/P2在此成功短点上未显示明确加速，符号
分析次数下降也不等于同等比例的总耗时下降。停滞检测在此点不触发，收益
由前述完整曲线的失败60→12更新对照证明。本轮保留这些选项的独立开关，
不把它们全部晋级为生产默认；实际总性能主要改善来自IALMob精确屏蔽根复用。

## 与原生D0日志的时间和更新口径

原生`native_d0_work_audited.json`审计值：Vg4/Vg8漏压段各51次非零接受
推进、1次拒绝；线性尝试216/201次，其中正更新因子215/200次，后者各含
零压1次更新。Vela从合格零压种子开始，零压检查0次更新；两边均排除栅压
预偏置。Vela P3总更新358/318次，分别是原生正因子更新的1.665/1.590倍。
原生失败尝试可能包含更新，故不将“总更新除接受步数”误记为成功步平均。

| 时间口径 | Vg4 / s | Vg8 / s |
|---|---:|---:|
| Sentaurus日志Solver总计 | 702.96 | 653.47 |
| Vela P3求解子进程累计墙钟 | 1250.68 | 623.52 |
| Vela P3求解子进程累计CPU | 870.95 | 602.48 |

原生日志记录4个可见处理器，但组装线程和求解线程均为1；本轮Vela使用
单一Eigen SparseLU/COLAMD路径。Windows与原生虚拟机环境不同，且日志
Solver计时与Vela子进程计时边界不同。上述数据说明耗时已接近同一量级，
不能据Vg8的数值宣称同硬件下已快于Sentaurus，也不能省略父运行器开销。
剩余更新差距主要还在每次推进的非线性求解，而非Vela推进次数更多。

## 来源与复现

`source_integrity.json`核对N1/P1/P2/gprof/P3冻结程序及逐文件源快照、三版
完整曲线输入清单；无不匹配。当前968个P3源文件及Release程序与冻结版本
一致。各版输出独立保留，未覆盖旧基线。实现和回归位于受版本管理的准备
脚本、物理接口、四方程工具和测试中；生成输出与冻结程序留在忽略目录。

从当前worktree根，可用受版本管理的运行器重放最终版本，例如Vg4：

```powershell
D:/msys64/ucrt64/bin/python.exe -X utf8 scripts/run_templates_ldmos_electrothermal_curve.py --probe reference_staging/templates_ldmos_native_poisson_20260913/binary_p3/electrothermal_probe.exe --input reference_staging/templates_ldmos_native_poisson_20260913/p3_vg4_zero.json --bias-points reference_staging/templates_ldmos_native_poisson_20260913/biases.json --output reference_staging/new_unique_p3_vg4 --growth-newton 12 --predictor linear
```

Vg8改用`p3_vg8_zero.json`和新的独立输出目录。输入清单中的外部网格、原生
几何导出、参考状态及冻结程序必须同时保留；四项性能开关均在输入中显式
开启，不更改生产DC默认行为。

## 尚未认领的范围

几何对齐后，原默认Fermi/BGN局部函数差仍存在；此次没有更换原生统计选项，
也没有拟合材料常数。密度分解中的温度、电势、QF项按指定顺序构造，存在
明显抵消，不能将各项RMS当成可相加的因果占比。六种源项替换只排除了当前
固定状态总体连续性残差由复合源主导的解释；原生边电流、界面插值及其与
默认统计模型的耦合仍需更细的通量证据。局部场接近不等于所有内部算法一致。

本轮资格限原10241节点网格、显式有限空穴接触和关闭Auger生成的独立D0入口，
沿用原Solve未启用Thermodynamic/Peltier/RecGenHeat的口径。生产DC默认电热
集成、额外量子或热输运模型不由本轮结果自动获得资格。
