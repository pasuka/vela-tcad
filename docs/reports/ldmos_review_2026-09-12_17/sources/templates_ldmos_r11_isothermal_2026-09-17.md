# R11 版本 G3/D5/D4 完整等温曲线复核

用户要求以 R11 重新运行全部 G3/D5/D4 等温长曲线。本次固定源码提交
`2a6a433b8f736e6c01d082733ba6f68d1ffb293b`，使用该版本 UCRT64 Release
运行器，从各算例原定义初值完整推进，共 G3 31 点、D5 62 点、D4 62 点。
未使用历史高漏压精确状态作为各点初值，未把逐点重闭合计作完整推进。

## 范围及冻结条件

- 运行器 SHA256：`defea9c1aadb5756bc0ccf637918342e2bddbc1b3a92f92495f80d4eb0c94ab7`。
  MSYS2 UCRT64、Release `-O3 -DNDEBUG`、无 gprof，显式
  `VELA_LINEAR_SOLVER=sparselu`，Eigen SparseLU/COLAMD。所有曲线串行执行。
- G3 从既有预偏置种子开始，以 Vd=0.1 V 扫 Vg=0–5 V 的 31 个原精确点。
  使用 R6 共享路径复核的冻结 G3 输入，仅重写输出路径；不含从中性状态
  生成预偏置种子的成本。保留原六项评分门限及数值块门限。
- D5/D4 各 Vg=4/8 V，由冻结零漏压种子在当前模型下重新闭合，随后
  由 Vd=0 推进至 40 V，输出原 31 个精确点；包含全部中间步、失败恢复、
  预测检查和参考系平移检查。保留各自旧步长与数值门限。
- 采用 `linked_d5_auger_no_generation_inputs.json` 和独立 D4 清单：高精度
  Fermi/BGN、来源一致 300 K 材料、Auger H/N0 及关闭 Auger 生成项。
  原网格 10,241 节点、AverageBox 输运和材料局部 Poisson 体积保持不变。
  unit_scaling 输入使用 µm、cm⁻³；曲线评分电流 A/µm。
- “R11”在此表示最终代码版本。R11 D0 profile 是独立电热配置，不能覆盖
  G3/D5/D4 的等温物理合同。显式高场开关目前接入电热温度路径，生成低场
  仍关闭；本次不宣称 D0 的高场加速比例可直接转移至等温入口。
- 继续使用冻结 Sentaurus 参考曲线；本轮不重跑原生，不新增同 VM 性能配对。
  Windows 完整推进时间独立报告，不与此前 D0 的 Linux 配对混用。

## 运行与证据

入口：[run_templates_ldmos_r11_isothermal.py](../../scripts/run_templates_ldmos_r11_isothermal.py)。
调用既有 G3 评分及 D5/D4 连续扫压、审计和双栅压联合评分脚本，不改物理实现。
运行前校验所有输入清单哈希，复制当前运行器与源码快照；拒绝覆盖旧输出。

```powershell
$env:Path = "D:\msys64\ucrt64\bin;D:\msys64\usr\bin;$env:Path"
python -X utf8 scripts/run_templates_ldmos_r11_isothermal.py --runner build-release/vela_example_runner.exe --output reference_staging/templates_ldmos_r11_isothermal_20260917
```

证据根为 `reference_staging/templates_ldmos_r11_isothermal_20260917/`，
包含 `binary/manifest.json`、`source/`、每条曲线的所有请求和原始日志、
`summary.json` 及联合评分。实际运行记录以这些文件为准。

## 执行状态

输入哈希预检通过，相关现有 linked/G3 评分回归 16/16 通过。
G3、D5 及 D4 全部完成，155/155 精确点通过原门限；两组双栅压工程及最终
联合评分均通过。运行器正常退出，完整性审计通过，未修改物理、默认值或门限。

| 已完成曲线 | 精确点 | Newton 更新 | 外部墙钟 / s | 原门限 |
|---|---:|---:|---:|---|
| G3 Id–Vg | 31 | 131 | 88.273 | 原六门限及残差块通过 |
| D5 Vg4 | 31 | 1131 | 865.790 | 通过；0 回退 |
| D5 Vg8 | 31 | 1086 | 841.211 | 通过；0 回退 |
| D4 Vg4 | 31 | 1127 | 1397.298 | 通过；0 回退 |
| D4 Vg8 | 31 | 1073 | 1329.923 | 通过；0 回退 |

G3 阈值误差 0.0107524 V、最大跨导相对误差 1.61008%、强反型端点误差
0.804273%；电流与 R6 共享路径复核逐点相同。
D5 最大电流误差 1.592417%/1.625083%，40 V 双栅压电流比误差 0.340154%，
最大归一化 KCL 不平衡 `1.404e-10%`，原工程及最终联合门限均通过。
两条 D5 均有 253 次点服务请求，26.666667 V 参考系切换等价性通过。
D4 最大电流误差 1.891761%/1.801547%，40 V 双栅压电流比误差 0.310958%，
最大归一化 KCL 不平衡 `5.953e-12%`，原工程及最终联合门限均通过。
四条 Id–Vd 曲线各有 253 次点服务请求，全部从零漏压重新闭合后连续推进，
0 次失败回退；26.666667 V 参考系切换等价性全部通过。评分中的
`resolved_point_count=30` 指非零电流相对误差适用的点数；零漏压仍实际求解、
检查并保留在 31 个精确点中，不是漏算。

与 R6 共享路径的精确状态重闭合电流对照，D5 Vg4/Vg8 最大相对变化分别为
`1.376e-12`/`2.087e-12`，D4 为 `7.935e-14`/`5.937e-14`；G3 电流逐值相同。
这补齐最终源码版本的完整等温推进资格，不把旧重闭合耗时当作完整推进对照。
汇总及逐点证据见 `completion_readout.json`、`g3/qualification.json`、
`d5_joint/summary.json`、`d4_joint/summary.json` 及各曲线 `audit_summary.json`。

## 计时及资格边界

G3 墙钟是直接运行器耗时，不含历史预偏置种子生成及后续评分；D5/D4 是外层
连续扫压驱动墙钟，包含本轮零漏压闭合、中间推进、输出及评分审计。对应 CPU
分别为 75.094、769.531、743.250、1266.891、1201.203 s；后四项合计驱动与
子进程 CPU。各条只有本轮一次完整执行，不新增重复性能配对资格。

旧等温控制器的步长及每点新进程协议保持不变，这里的更新数不能直接与 D0
R11 电热控制器的 344/303 次对比。本轮没有重新运行 Sentaurus，因此不认领
新的同条件性能比或等温加速比例。局部密度/带边与热学资格仍引用其独立 D0
证据，本轮新增的是 G3/D5/D4 原曲线及数值门限的最终源码回归资格。

本轮相关 linked/G3 脚本回归 16/16 通过。此前该源码提交的 Release 全量
834/834 CTest 资格保留；本轮无 C++ 修改，没有重复运行整套 CTest。
