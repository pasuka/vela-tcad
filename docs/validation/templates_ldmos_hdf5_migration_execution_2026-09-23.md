# HDF5 生产状态迁移执行记录

本任务按用户授权在 `templates-ldmos-phase-a` worktree 修改，在 T470p 编译和运行。
对应[迁移方案](templates_ldmos_hdf5_production_migration_plan_2026-09-23.md)。
状态：T470p 的功能、恢复和最终三轮性能验收已通过。代码默认使用新 HDF5
状态；C560 的 Linux Debug 构建及完整 CTest 已通过，本轮修改整理提交至当前工作分支。

## 冻结与范围

- 本地行政证据：`build/hdf5_migration_20260923/`。保留修改前 diff、文件散列、
  状态入口清单与依赖解析清单；Git HEAD 不能代替有未提交修改的源文件快照。
- T470p 独立目录：`D:/code-repo/vela-bench/ldmos_hdf5_migration_20260923/`。
  Release / MSYS2 UCRT64；短点资格使用 UMFPACK、求解器/BLAS 1/1 线程。
- 150 项 manifest 依赖中，148 项从实际文件或受保护依赖表核对散列。
  另外两项为历史 BJT 的 `examples/BJT/step1.inp`、`step2.inp`，本次未取得，
  不属于当前 LDMOS 运行输入；不据此认领历史 BJT 原始输入重演通过。
- 曲线/掺杂 CSV、配置/ledger JSON 不作状态格式替换。

## 已完成的接口与验证

新增 `vela.state/2` C++/Python 合同，磁盘值为明确单位的 binary64。
网格指纹包含编号、顺序、坐标、连接关系、区域、接触和长度单位；保存独立的
QF 参考值及增量。新内核不经 VDS1 缓冲中转，不要求 FlatBuffers。
写入使用同目录临时文件，关闭成功后替换；写入失败保留原检查点。

T470p 已通过：

| 检查 | 结果 |
|---|---|
| C++ 存储合同、C++/Python 双向互读、DC worker 网格/参考系错误隔离 | 3 个 CTest 入口全部通过 |
| Python 状态、参考系、linked driver、便携算例合同 | 23 项，22 通过、1 项因未提供 C++ 工具跳过；双向互读已由上述 CTest 独立覆盖 |
| 新 schema、Vg4、前 8 精确点 | 通过，父驱动墙钟 173.1834 s |
| 新 schema、Vg8、前 8 精确点 | 通过，父驱动墙钟 173.0557 s |
| 七份不同种子的一次性转换 | 全部解码字段与源 CSV 解析值逐字节一致 |

短点使用 `dd_qualification/vg{4,8}_8_r2`；这是功能资格，尚未与同一冻结控制程序
交错配对，不能用来宣布新实现的性能达标。此前两次准备失败分别是驱动 manifest
缺少后端标识、编译未启用显式 OpenBLAS 线程控制；均在正式 Newton 求解前失败，
日志保留，不计入通过的短点耗时。

## 当前迁移状态

十组 LDMOS linked 输入清单已引用新种子，并更新实际文件散列。三份仓库内被取代的
CSV 种子已核对源/目标散列、转换字段一致性和活跃引用后退役：便携 D5 两份，
PN2D 拒绝推进夹具一份，清单为 `retired_seeds.json`。来源散列及历史说明保留；
隔离性能控制目录中的旧格式输入不是生产依赖，仍按冻结计划保留。
单点 Newton/诊断入口已接入与扫压相同的网格及参考系作用域。
普通/量子工作流和 PN2D、BJT、Schottky、TransportModels 的状态路径及相关测试
已迁移。T470p Release 全量构建通过，修复测试包缺文件和旧格式断言后，
`ctest_m3_repair.log` 为 **850/850 通过，152.48 s**。测试通过不等于这些器件的
所有长曲线已在新格式重新取得资格。

T470p 已安装的 h5py 3.16.0 原先按 HDF5 2.1.1 编译，实际加载 2.2.0；
短点出现版本警告。已在任务私有目录重新构建 h5py，编译和运行 HDF5 均为 2.2.0；
未修改系统安装。对应 wheel 的 SHA-256 为
`3802e7645fa6a4dce39d33228d645a47aba4142008a209bfb4f0222c95588b54`。
构建使用 h5py 3.16.0 源码、私有 Cython 3.3.0，修正了 Windows HDF5 DLL 两位编号
匹配及 LZF 的 `_int64` 拼写；实际构建日志为 `h5py_build_v4.log`。
后续测试均设置私有 `python_runtime` 路径。此前短点的旧运行库口径单独保留。

## 电热接入进度

`ElectrothermalState` 将四方程向量、分离 QF 参考/增量和温度保存到独立 HDF5，
JSON 保留控制、诊断和文件引用。电热模式的密度不是恢复未知量，允许省略密度对，
不生成伪造的零密度；DD 模式仍要求电子、空穴密度。嵌套预测候选同样迁移。
输入模式与参考系校验、文件散列核验、历史状态恢复和不可覆盖的状态文件已接入。
完成态 resume 也重新验证已接受历史，不能仅凭 ledger 的 complete 放行。

T470p `ctest_m4_r3.log`：5 个入口通过，20.74 s；其中电热运行器含 43 项检查。
随后补加缺失温度、损坏文件拒绝测试，`ctest_m4_complete.log` 再次通过。
真实 D0 比较使用哈希核对的 R11 输入、UMFPACK 1/1、双栅压 0--40 V，每条31精确点。
旧 JSON 控制仅在独立目录运行保留的迁移前二进制；新 HDF5 曲线从零压检查点恢复，
逐点比较状态及温度，再应用既有原生电学/热学门限。进度见远程 `d0_status.json`。

## 第一轮电热完整对照

`d0_status.json` 已完成。旧 JSON 控制与首版 HDF5 候选均完成双栅压 62 精确点，
HDF5 路径先停止于零压检查点，再恢复到 40 V。`d0_acceptance.json` 通过既有
电学和热学门限。逐点物理状态、参考状态、两类 QF 参考及温度最大绝对差均为 0。
两者精确点记录的 Newton 更新合计分别为 Vg4 272 次、Vg8
253 次；这不是包含全部中间推进的总更新数。

| 栅压 | 旧 JSON 控制墙钟 / s | HDF5 暂停＋恢复墙钟 / s | 原生曲线最大电流误差 |
|---|---:|---:|---:|
| 4 V | 195.6175 | 199.6087 | 0.0240871% |
| 8 V | 179.5568 | 185.3405 | 0.00714390% |

这是单次电热功能对照，不能替代三轮 VDS1/HDF5 性能验收。Vg8/40 V 的原生峰值
温度为 513.568615 K，候选为 513.526633 K，差 0.041982 K。最终源码增加来源
校验缓存后仍须重新核验；新一轮使用 `final_curves/`，不覆盖上述证据。

## 来源记录、分发与性能复测

接口文档为 [Restart state storage](../state_archive.md)。补齐 DD 偏压/角色、
预测父状态散列、电热输入来源与 resume 文件散列校验。独立运行包携带实际 DLL
依赖，在去掉 UCRT64 开发 PATH 后验证读取 HDF5，记录 `package_smoke.json`。
Python 驱动仍需匹配的 Python/numpy/h5py 环境；不把 C++ DLL 包称为完整 Python
独立分发包。

首轮同条件短点配对 `final_qualification/preflight8/`：VDS1 双栅压合计
275.8744 s，HDF5 合计 308.0707 s，增幅 **11.6706%**。解码状态、轨迹与计数
一致，性能不合格。定位到 Python 将数值逐个转为文本又读回、重复计算来源散列；
改为保留 binary64 数值，以及核对输入字节后复用来源散列。没有修改 Newton、
物理模型或验收门限。

修正后全量 CTest 为 849/850，剩余夹具的材料名称与原生导出不一致；修正为原生
`Si/SiO2` 后对应入口 1/1 通过（14 项 Python 检查）。证据分别为
`ctest_m5_cost_fix.log` 和 `ctest_m5_fixture_fix.log`。之前的
`ctest_m5_repair.log` 曾全量 850/850 通过，不能代称修正后的单次全量运行结果。

当前最终包冻结于 `final_qualification/plan_v2.json`，首个串行驱动状态为
`final_pipeline_status.json`（最新所有者见下文）。先复跑四条前 8 点曲线，再跑 G3/D4/D0、冻结
300 K 检查及十二条三轮 D5 配对。短点 5% 仅是进入长曲线的排程筛选，最终
性能门限仍是用户批准的三轮双栅压合计增幅不超过 3%。

第二版 `preflight8_v2/analysis.json` 已完成，四条短曲线全部通过数值一致性检查：

| 栅压 | VDS1 / s | HDF5 / s | 增幅 |
|---|---:|---:|---:|
| 4 V | 138.2408 | 138.5737 | 0.2408% |
| 8 V | 137.5599 | 138.6412 | 0.7861% |
| 合计 | 275.8008 | 277.2149 | 0.5127% |

每条保存 128 个状态文件，VDS1 为 104,870,912 字节，HDF5 为 106,178,560
字节。`performance_qualified=false` 是正确的：该报告仅有一轮前 8 点，尚不满足
完整三轮条件。最终 G3 的 31 点及既有指标已通过（求解墙钟 45.775 s）；最终 D4 结果见下文。

外围入口核查补齐了 SingleDevice、BVmethods 延续、Slot LDMOS 阶段生成器、
BJT 网格敏感性种子和 PN2D VTK 再启动导出。VTK 导出明确校验节点坐标/顺序，
按原生 cm^-3 输入的 BJT 种子显式转换为 m^-3；曲线、掺杂及网格 CSV 未替换。
这批 22 个文件由 `extra_entry_manifest_v2.json` 冻结，未改变运行中的计时依赖。
完整回归在首个串行驱动退出后运行，状态见 `post_pipeline_tests.json`，
`ctest_final_entries.log` 为 **850/850 通过，161.59 s**，覆盖这批外围入口修改和
旧生产种子删除。未与仿真同时争用计时 CPU。

## 最终功能复核与 300 K 初态修正

最终包已完成 G3 31 点、D4 双栅压 62 点及 D0 双栅压 62 点。D4 原工程/最终
联合验收通过：Vg4/Vg8 最大电流误差 1.89176% / 1.80155%，终端电流比误差
0.310958%。D0 逐点物理状态、参考状态、QF 参考、温度及精确点 Newton 更新与
旧 JSON 控制一致，既有电学/热学门限通过。D0 的零压暂停后恢复保留了原检查点。

固定 300 K 的第一种检查路径未通过：直接把自热中压点的温度改为 300 K，
Vg4/20 V 出现 `line_search_failed`，6 次接受更新后空穴 QF 方向持续增大；
低压点及其恢复已通过。该失败保留在 `final_curves/cold300/`，不能称为
300 K 资格通过，也不据此放宽门限或修改 Newton 算法。

第二种检查从 neutral_300K 零压开始，但旧 JSON 生产电热驱动仍拒绝非零偏压：
电学已收敛，拒绝原因仅为 `heat_balance`。全节点恒温约束引入分布式热库，
不能用仅统计原热接触通量的自热判据验收；该证据留在 `cold300_continuation/`。

因此固定温度检查使用独立点驱动 `cold300_point_driver.py`，从已合格的 300 K
零压 HDF5 状态连续推进，保持原电学块、逐行及 KCL 门限，严格要求全部节点
300 K，并复核低/中/高压保存恢复。报告明确为 `frozen_temperature_electrical`；
原 D0 自热门限及其已通过资格不变，不把恒温热库结果认作物理自热验收。

`cold300_electrical/` 已完成双栅压 62 精确点，全部节点严格为 300 K；
两条扫描墙钟分别为 410.6004 s / 398.6610 s。每档在 1.333333、20、40 V
冷读恢复，六次均为 0 次 Newton 更新、状态差 0；独立复核 KCL 最大相对差
7.70e-14，低于原 1e-3 门限。证据为 `summary.json`、`restart_comparison.json`
及 `restart_kcl.json`，已下载到本地行政证据同名子目录。

最终串行所有者为 `resumed_pipeline_r2_status.json`，在北京时间 2026-09-23
14:43:11 完成 `final_qualification/full31_v2/` 的三轮完整配对。前两份 pipeline 的
review_required 保留为检查路径修正的证据，不能误当新队列状态。最终程序未改变。

最终引用清单 `final_inventory_v2.json` 覆盖 1538 个源文件/输入，包含 96 处配置状态
引用：80 处明确 `.h5` 路径，16 处为模板占位符或由 bundle 解析的 `@seed`；
无旧格式状态路径，无随算例分发的 CSV 状态文件，无内嵌电热状态数组。
生成型 HDF5 输出在首次运行前不存在是正常情况。该清单不扫描历史证据目录，
也不将保留的历史格式实验脚本认作生产格式兼容接口。
本地 `final_source.tar` 保存该清单对应的源文件/输入快照，大小 58,931,200 字节，
SHA-256 为 `70d5bd6799c954bf21e235679adef086659758bfe468cfc0f1b5e28fcadead84`。
该归档不是模拟输出或最终重复计时证据的替代品。

## 最终三轮完整计时：通过

冻结的 `vela.state/2` 实现完成 12/12 条 D5 曲线、372 个精确点，六组双栅压
联合验收通过；跨格式与跨轮的解码字段、推进轨迹和计算计数一致。全部曲线
回退为 0，Vg4/Vg8 的 Newton 更新在两格式各轮均为 1123/1076。

| 轮次 | 栅压 / V | VDS1 / s | HDF5 / s | HDF5 增幅 |
|---|---:|---:|---:|---:|
| 1 | 4 | 445.2683 | 446.6442 | 0.3090% |
| 1 | 8 | 445.8210 | 450.9533 | 1.1512% |
| 2 | 4 | 449.0836 | 450.2466 | 0.2590% |
| 2 | 8 | 448.7669 | 447.9859 | -0.1740% |
| 3 | 4 | 448.4259 | 449.5010 | 0.2397% |
| 3 | 8 | 447.6071 | 451.9185 | 0.9632% |
| 合计 | 双栅压 | 2684.9728 | 2697.2496 | **0.4572%** |

三轮合计增幅低于已批准的 3% 门限，`performance_qualified=true`。
分栅压合计增幅为 0.2691% / 0.6454%。每条曲线均保存 476 个状态文件；
VDS1 为 389,988,704 字节，HDF5 为 394,851,520 字节。保存频率及数值门限未变。
本结果独立于历史 `vela.ddstate/1` 的 0.8695%，不混用两份资格。

完成后确认远端批次及求解进程已退出。`analysis.json`、`summary.json` 和
`resumed_pipeline_r2_status.json` 已下载到本地行政证据的 `full31_v2/`，逐份
SHA-256 与远端一致。原始逐点输出仍在 T470p 的冻结目录，没有再次打包全部历史状态。
分析报告 SHA-256：`194cd753a56074632e5c13003494b0e8b85e4c862dba11e872209492a35591a8`。

后续在 `qzw-Lenovo-C560.local` 完成独立 Linux Debug 构建及 850/850 CTest，见
[Linux CI 报告](templates_ldmos_linux_ci_2026-09-23.md)。该资格采用 Ubuntu 26.04 /
GCC 15 / SparseLU；与 GitHub Actions 的 Ubuntu 24.04 / GCC 16 分别记录。

## 本地分支整理

本轮提交包含已验证的 HDF5 状态接口、活跃算例及七份种子迁移、依赖配置、
测试和性能诊断记录。旧格式实验源文件保留用于历史对照，未加入生产构建；
模拟输出及本地/远端构建目录不入库。装配结构和 Fermi 节点缓存仍为显式候选。

提交前补齐冻结清单依赖的 Git 字节保护，防止 autocrlf 在跨平台检出时改变
JSON/CSV 输入而破坏 SHA-256 和 HDF5 网格身份校验；未重算物理基线或放宽门限。
程序源文件沿用通过 T470p Release 和 C560 Debug 各 850 项测试的版本。

便携 D5 来源记录同步更新当前 HDF5 输入清单散列，旧值另存于
`previously_recorded_source_bundle_sha256`，以区分迁移前后来源。

最终暂存快照检查 63 项文件散列（30 个唯一依赖）全部通过；在 C560 独立
`staged_review` 目录运行 linked 输入、生产导出、局部场与 HDF5 双向互读
共 35 项测试全部通过（0.412 s）。程序源文件未再修改；该补充检查验证了
Git 暂存字节而非仅工作目录，日志为远端 `staged_review.log`。
快照 SHA-256：`6e1ef000fb237d6165240f32b9a0e66a61ffa746e792584fc9cf5237edf2e237`。
